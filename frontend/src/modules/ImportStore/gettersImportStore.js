export function getContractCashDetail (state) {
  // console.log('import get: ', state, state.importList)
  return state.importList
}

export function getImportHistory (state) {
  // console.log('import get: ', state, state.importList)
  return state.importHistory
}

export function getLastUploadDate (state) {
  // console.log('import get: ', state, state.importList)
  return state.lastUploadDate
}
